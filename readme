create web app using flask with react js
pipenv shell -> to create virtual env
pipenv install flask flask_restx flask_sqlalchemy flask_jwt_extended
$env:FLASK_APP = "main.py"
then falsk shell
db for install the db
to create database db.create_all()


to create serializer method
to create and freeze the requirements file  pip freeze > requirements.txt
to migrate the db pipenv install flask_migrate
to migrate flask db init
to add table flask db migrate -m "Initial migration"
flask db upgrade
